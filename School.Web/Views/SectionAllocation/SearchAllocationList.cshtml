<table class="table">
    <tr>
        <th>
            Action
        </th>
        <th>
            @Html.DisplayName("StudentName")
        </th>
        <th>
            @Html.DisplayName("MobileNo")
        </th>
        <th>
            @Html.DisplayName("EmailId")
        </th>
    </tr>

    @foreach (var item in ViewBag.AllocationDetail)
    {
        <tr>
            @*<td>
                    @Html.DisplayFor(modelItem => item.SectionAllocationId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SessionId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.EnquiryId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AdmissionId)
                </td>*@
            <td>
                @Html.CheckBox("AdmissionId", new { value = item.AdmissionId, name = "SelectedFruits" })
            </td> 
            <td>
                @item.StudentName
            </td>
            <td>
                @item.MobileNo
            </td>
            <td>
                @item.EmailId
            </td>            
        </tr>
    }

</table>